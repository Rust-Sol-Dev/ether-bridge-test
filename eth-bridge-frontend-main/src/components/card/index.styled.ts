import styled from "styled-components";

const Container = styled.div`
  padding: 20px 36px;
  background-image: linear-gradient(to bottom, #101528, #3d3e5c);
  width: 320px;
  border-radius: 24px;
  display: grid;
  grid-template-rows: auto auto auto auto;
  gap: 36px;
`;

export { Container };
